spring:
  application:
    name: github-data-aggregator

logging:
  level:
    com.jdl.ghdata: info

github:
  api:
    base-url: https://api.github.com
    cache:
      ttl: PT1M
      max-size: 1000
